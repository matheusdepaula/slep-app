/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Initial from './src/scenes/initial/index';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Initial);
